# minifyEnabled is false for both build types, so nothing here runs yet. The file exists
# because build.gradle names it, and because the first thing anyone turning minification
# on will need is somewhere to keep Chaquopy's and ONNX Runtime's reflection rules.
